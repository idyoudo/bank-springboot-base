server:
  port: 8081

springboot:
  property:
    name: actuator
    address: I am in SpringBoot

spring:
  application:
    name: bank-springboot-base-actuator-admin-client
  boot:
    admin:
      client:
        url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"